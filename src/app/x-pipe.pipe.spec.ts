/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { XPipePipe } from './x-pipe.pipe';

describe('XPipePipe', () => {
  it('create an instance', () => {
    let pipe = new XPipePipe();
    expect(pipe).toBeTruthy();
  });
});
